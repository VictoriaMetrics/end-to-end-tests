kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  name: vlinsert-pod-failure
spec:
  selector:
    namespaces:
      - vm
    labelSelectors:
      app.kubernetes.io/name: vlinsert
  mode: all
  action: pod-failure
  duration: 7m
