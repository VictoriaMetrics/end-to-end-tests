kind: StressChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  name: vlselect-cpu-usage
spec:
  selector:
    namespaces:
      - vm
    labelSelectors:
      app.kubernetes.io/name: vlselect
  mode: all
  stressors:
    cpu:
      load: 100
      workers: 16
  duration: 5m
